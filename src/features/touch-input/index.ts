/* Touch input support — public entry point (pure helpers). */

export {
  TAP_MOVE_THRESHOLD,
  isWithinTapThreshold,
  TOOLBOX_TOUCH_DROP_EVENT,
} from './touchInput';

export type { Point, ToolboxTouchDropDetail } from './touchInput';
